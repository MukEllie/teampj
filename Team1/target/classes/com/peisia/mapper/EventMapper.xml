<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.peisia.mapper.EventMapper">

    <select id="getEvent" resultType="com.peisia.dto.EventDto" parameterType="int">
        SELECT * FROM g_event WHERE e_id = #{id}
    </select>
    
    <!-- 던전 레벨에 맞는 e_type 필터로 이벤트 id 리스트 조회 -->
    <select id="getEventIdsByTypes" resultType="int" parameterType="list">
        SELECT e_id FROM g_event 
        WHERE e_type IN
        <foreach collection="list" item="type" open="(" separator="," close=")">
            #{type}
        </foreach>
    </select>

</mapper>